/**
 * Споделен layout за /for-home и /for-home/[slug].
 * SEO и JSON-LD за главната страница за доставка са в page.js, за да не се дублират грешно на категориите.
 */
export default function ForHomeLayout({ children }) {
  return children;
}
